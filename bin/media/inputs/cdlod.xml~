<?xml version='1.0' encoding='utf-8'?>
<data>
  <!-- initial camera world position x (right), y (up), z (from) coordinates) -->
  <position>
    <x>11.0</x>
    <y>3.0</y>
    <z>-3.0</z>
  </position>
  <!-- initial camera direction yaw (horizontal), pitch (vertical) in radians -->
  <yaw>-1.44</yaw>
  <pitch>-0.385</pitch>
  <!-- camera speed (float) -->
  <speed>0.8</speed>
  <!-- max lod depth (max number of patches is 4^LOD) -->
  <max_lod>7</max_lod>
  <!-- max tessellation level (max tessellation is 2^GRID) -->
  <max_grid>4</max_grid>
  <!-- maximum viewing distance -->
  <max_distance>41.0</max_distance>
  <!-- terrain size -->
  <terrain_size>16.0</terrain_size>
  <terrain_height>1.6</terrain_height>
  <!-- heightmap file, size must be 2^(max_grid + max_lod) x 2^(max_grid + max_lod) -->
  <!-- red and green channels contains the 16 bit height data, blue and alpha the normal data -->
  <heightmap_file>heightmap.png</heightmap_file>
</data>
